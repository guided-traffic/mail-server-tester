apiVersion: v1
kind: Service
metadata:
  name: {{ include "mail-server-tester.fullname" . }}
  labels:
    {{- include "mail-server-tester.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mail-server-tester.selectorLabels" . | nindent 4 }}
